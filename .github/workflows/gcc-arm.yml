name: gcc-arm

on: [push]

jobs:
  build:

    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@master
    - name: make clean
      run: make clean
    - name: make all
      run: make all
